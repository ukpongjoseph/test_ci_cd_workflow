name : Hello workflow

on : 
  push :
    branches :
      - main

jobs :
  Hello world job : 
    runs-on : ubuntu-latest
    steps :
      - name : init step
        run : |
          echo "Hello world"
