name : Hello workflow

on : 
  push :
    branches :
      - main

jobs :
  Hello_world_job : 
    runs-on : ubuntu-latest
    steps :
      - name : init step
        run : |
          echo "Hello world"
